# JavaScript学习笔记



## JavaScript概述

JavaScript是一门解释型编程语言，解释型编程语言的代码不需要手动编译，而是通过解释器边编译边执行，所以要运行JS我们必须要在计算机中安装JS的解释器（有浏览器就行）。

JavaScript是一个单线程的编程语言，简言之，JS同一时间只能做一件事，一件事完成才会继续做另一件事。单线程降低了JS代码的复杂度，也在某种场景下1使得JS性能变差，所以JS又为我们提供了异步的编程方式，以提高代码的运行速度

## 引用JS外部文件

```
<script src="script.js"></script>
```

## 01.javascript基础语句

```javascript
console.log("xxxxx");//会在控制台输出
document.write("xxxxx")//会在网页中输出
//第一种:输出内容用""括起，直接输出""号内的内容。
<script type="text/javascript">
  document.write("I love JavaScript！"); //内容用""括起来，""里的内容直接输出。
</script>

//第二种:通过变量，输出内容
<script type="text/javascript">
  var mystr="hello world!";
  document.write(mystr);  //直接写变量名，输出变量存储的内容。
</script>

//第三种:输出多项内容，内容之间用+号连接。
<script type="text/javascript">
  var mystr="hello";
  document.write(mystr+"I love JavaScript"); //多项内容之间用+号连接
</script>

//第四种:输出HTML标签，并起作用，标签使用""括起来。
<script type="text/javascript">
  var mystr="hello";
document.write(mystr+"<br>");//输出hello后，输出一个换行符
  document.write("JavaScript");
</script>

//关于JS输出空格问题，如果想要实现输出空格，可以使用特殊字符“ ”实现
<script type="text/javascript">
  document.write("&nbsp;");//输出空格
</script>
```

## 02.JS编写位置

```javascript
1.可以把JS编写到网页内部的script标签中
2.外部的JS文件，然后通过script标签连接
	<script src="./script/script.js"></script>
注意：script标签内只能有一种形式，也就是说如果用了script外部式，则标签内不能再写JS代码
3.可以吧JS代码编写到指定属性中
	<button onclick="alert('xxxxxx')">点我一下</button>
我们可以将JavaScript代码放在html文件中任何位置，但是我们一般放在网页的head或者body部分。
//放在<head>部分
最常用的方式是在页面中head部分放置<script>元素，浏览器解析head部分就会执行这个代码，然后才解析页面的其余部分。
//放在<body>部分
JavaScript代码在网页读取到该语句的时候就会执行。
```

## 03.JS基本语法

```
1.多行注释/*
2.单行注释//
3.JS严格区分大小写
4.在JS中个多个空格和换行会被忽略(可以用这个特点对代码进行格式化)
5.JS中每条语句应该以分号结尾（JS中具有自动添加分号的机制，所以如果不写分号解释器会自动添加）
```

## 04.字面量和变量

```javascript
字面量：
字面量其实就是一个值它所代表的含义就是它的字面意思
在JS中所有的字面量都可以自由使用，但是直接使用字面量并不方便
变量需要先声明才能作为字面量使用，否则会报错
变量：
变量可以存储字面量
并且变量中存储的字面量可以随意修改
通过变量可以对字面量进行描述，且变量比较方便修改

eg：
let x
x=80
x=‘哈哈’

let age
age=80
age=81
console.log(age)，age输出为81，直接会被修改，之前的age不需要修改
(尽量把字面量存到变量中，用变量去表示字面量)
```

```
变量的使用：
声明变量：let 变量名/var 变量
变量赋值：a = xxxx
比较习惯于声明和赋值同时进行：let a = 80
```

## 05.变量的内存结构

```
内存大小是确定的，但存进去的东西的内存大小是不定的，存在内存溢出
字面量需要在开辟一片新的空间去存储，但是如果原来就有空间就不会再开辟
通过内存地址去寻找字面量所存的位置，所以变量对应的值中存储的是对应字面量的内存地址。这样来建立联系。
（变量并不存储任何值，而是存储内存地址）
不同的变量存储的内容一致时就不会再把相同的字面量开辟出一个新的空间，而是直接默认使用原来就有的字面量的内存地址
```

## 06.常量

```
声明常量：const xxxx = xxxxx
JS中，使用const声明常量，且常量只能被赋值一次，重复赋值会报错
在JS中除了常规的常量外，有一些对象类型的数据我们也会声明为常量
```

## 07.标识符

```
所有可以由我们自己自主命名的内容都可以认为是一个标识符
像变量名，函数名，类名等等
使用标识符的规范：
1.只能含有字母，数字，下划线，$，且不能以数字开头
2.不能是JS中的关键字和保留字，不建议使用内置的函数或类名作为变量名
3.命名规范:	1.首字母小写，每个单词开头大写（驼峰命名法）
	       2.常量的字母全大写
```

## 08.数据类型

```
1.数值（Number）
2.大整数（Bigint）
3.字符串（String）
4.布尔值（Boolean）
5.空值（Null）
6.未定义（Undefined）
7.符号（Symbol）
```

### 1.数值

```
在JS中所有的整数和浮点数都是Number类型
JS中的数字不是无限大，当数值超过一定数值时会显示近似值，而近似值就不确定了，可能大可能小。
在JS中进行精度比较高的计算需要注意
注意在二进制中，计算机无法精确的计算1/10的
计算机被难住以后也会说NaN（Not a Number）：当计算机也无法计算时会显示
，表示非法的数值。
```

### 2.大整数

```
用来表示一些比较大的整数
大整数使用n结尾，表示数字范围无限大（实际上肯定是有限的）
```

*其他进制的数字：二进制0b，八进制0o，十六进制0x。但是打印出来的还是十进制。

### 3.字符串

```
在JS中用单引号或者双引号来表示字符串
转义字符：
\"":""
\'':''
\\:\
\t：缩进，相当于Tab
\n：换行
模板字符串：
用反单引号来表示模板字符串`xxxxx`
模板字符串可以嵌入变量 `${xxxx}`
```

### 4.其它数据类型

```
布尔值（Boolean）
	进行逻辑判断，是有两个：true和false
空值（Null）
	表示空对象，空值只有一个null
	使用typeof检查一个空值时会返回一个“object”
	所以typeof无法检查空值
未定义（Undefined）
	当声明一个变量而没有赋值时，其值就是Undefined
	Undefined类型的值只有一个就是undefined
符号（Symbol）
	用来创建一个唯一的标识
```

### 类型检查

```
数值大整数不能混合运算
typeof 运算符，用它来检查值的类型，他会根据不同的值返回不同的值
他检查的是值的类型，而不是变量的类型
```

## 09.类型转换

```
类型转换值将一种数据类型转换成其他类型
```

## 10.判断语句（if...else）

if...else语句是在指定的条件成立时执行代码，在条件不成立时执行else后的代码。

**语法****:**

```
if(条件)
{ 条件成立时执行的代码 }
else
{ 条件不成立时执行的代码 }
```

假设我们通过年龄来判断是否为成年人，如年龄大于等于18岁，是成年人，否则不是成年人。**代码表示如下****:**

```
<script type="text/javascript">
   var myage = 18;
   if(myage>=18)  //myage>=18是判断条件
   { document.write("你是成年人。");}
   else  //否则年龄小于18
   { document.write("未满18岁，你不是成年人。");}
</script>
```

## 11.对话框

### 1.警告（alert 消息对话框）

我们在访问网站的时候，有时会突然弹出一个小窗口，上面写着一段提示信息文字。如果你不点击“确定”，就不能对网页做任何操作，这个小窗口就是使用alert实现的。

**语法:**

```
alert(字符串或变量);  
```

**看下面的代码:**

```
<script type="text/javascript">
   var mynum = 30;
   alert("hello!");
   alert(mynum);
</script>
```

**注:**alert弹出消息对话框(包含一个确定按钮)。

### 2.确认（confirm 消息对话框）

confirm 消息对话框通常用于允许用户做选择的动作，如：“你对吗？”等。弹出对话框(包括一个确定按钮和一个取消按钮)。

**语法:**

```
confirm(str);
```

**参数说明:**

```
str：在消息对话框中要显示的文本
返回值: Boolean值
```

**返回值:**

```
当用户点击"确定"按钮时，返回true
当用户点击"取消"按钮时，返回false
```

**注:** **1.通过返回值可以判断用户点击了什么按钮**

**2.消息对话框是排它的，即用户在点击对话框按钮前，不能进行任何其它操作。**

### 3.提问（prompt 消息对话框）

**`prompt`**弹出消息对话框,通常用于询问一些需要与用户交互的信息。弹出消息对话框（包含一个确定按钮、取消按钮与一个文本输入框）。

**语法:**

```
prompt(str1, str2);
```

**参数说明：**

```
str1: 要显示在消息对话框中的文本，不可修改
str2：文本框中的内容，可以修改
```

**返回值:**

```
1. 点击确定按钮，文本框中的内容将作为函数返回值
2. 点击取消按钮，将返回null
```

**注:在用户点击对话框的按钮前，不能进行任何其它操作。**

## 12.打开窗口

### 打开新窗口（window.open）

open() 方法可以查找一个已经存在或者新建的浏览器窗口。

**语法：**

```
window.open([URL], [窗口名称], [参数字符串])
```

**参数说明:**

```javascript
URL：可选参数，在窗口中要显示网页的网址或路径。如果省略这个参数，或者它的值是空字符串，那么窗口就不显示任何文档。
窗口名称：可选参数，被打开窗口的名称。
    1.该名称由字母、数字和下划线字符组成。
    2."_top"、"_blank"、"_self"具有特殊意义的名称。
       _blank：在新窗口显示目标网页
       _self：在当前窗口显示目标网页
       _top：框架网页中在上部窗口中显示目标网页
    3.相同 name 的窗口只能创建一个，要想创建多个窗口则 name 不能相同。
   4.name 不能包含有空格。
参数字符串：可选参数，设置窗口参数，各参数用逗号隔开。
```

### 关闭窗口（window.close）

close()关闭窗口

**用法：**

```
window.close();   //关闭本窗口
```

或

```
<窗口对象>.close();   //关闭指定的窗口
```

例如:关闭新建的窗口。

```javascript
<script type="text/javascript">
   var mywin=window.open('http://www.imooc.com'); //将新打的窗口对象，存储在变量mywin中
   mywin.close();
</script>
```

**注意:上面代码在打开新窗口的同时，关闭该窗口，看不到被打开的窗口。**

## 13.对象

```javascript
对象：
原始值只能用来表示一些简单的数据，不能表示复杂数据
而对象是JS中的一中复合数据类型，相当于一个容器，在对象中可以存储不同类型的数据。
let a = 10这就属于原始值，只能表示一些独立的数据
如：我们现在需要表示一个人的信息
//把很多变量放进一个“袋子”里，对象就是这个袋子
let name = "任超毅"
let age = 18
let gender = "男"
首先创建对象：
let obj = new Object()
/*
对象中可以存储多个各种类型的数据
	对象中存储的数据，我们称为属性
想对象中添加属性：
	对象.属性名 = 属性值
*/
obj.name = "任超毅"
obj.age = 18
obj.gender = "男"
console.log(obj)
输出为：{name: '任超毅', age: 18, gender: '男'}
//修改属性：直接改
//删除属性
delete obj.name

对象属性
/*属性名
	-通常属性名就是一个字符串，所以属性名可以是任何值，没有什么特殊要求
	-但是如果属性名太特殊，不能直接使用，需要使用中括号[],所以最后好还是	 按照标准规范命名格式，别把名字起得太奇葩
	
	-也可以使用符号作为属性名
		获取属性的时候也必须使用symbol
		使用symbol添加属性，通常是那些不希望被外界访问的属性
		
	-使用[]操作属性时，可以使用变量（变量名左右两侧不要加引号）
	let str = "address"
	obj[str] = "花果山" 等价于 obj["address"] = "花果山"
					   也等价于 obj.address = "花果山"
	obj.str = "哈哈"，使用.的形式添加属性时，不能使用变量
*/
let mySymbol = Symbol()
obj[mySymbol] = "通过symbol添加属性"
console.log(obj[mySymbol])

/*属性值
	对象的属性值可以是任意的数据类型，也可以是一个对象（无限套娃）
*/
obj.f = object()
obj.f.name = "任超毅"
console.log(obj.f.name)

/*
使用typeof检查对象时，会返回object
in 运算符
	-用来检查对象中是否有某个属性
	-语法 xxxx in obj
	-如果有则返回true，没有则返回false
*/

对象的字面量
/*
对象字面量
	-可以直接使用{}来创建对象
	-使用{}所创建的对象，可以直接向对象中添加属性
	-语法：
	{
		属性名：属性值，
        [属性名]：属性值，
	}
*/
let obj = {
    name:"任超毅",
    age:18,
    ["gender"]:"男"
    [mySymbol]:"特殊属性"
    hello：{
    	xxxxxx
    	xxxxxx
	}
}

枚举属性
/*举属性，指将对象中的所有属性全部获取
	for-in语句
	-语法：
		for(let propName in 对象{
			语句
		}
	- for-in 的循环体会执行多次，有几个属性就会执行几次
		每次执行时都会将属性名赋值给我们所定义的变量
	-注意：并不是所有的属性都可以枚举，比如使用符号添加的属性	
*/
let obj = {
    name: '任超毅',
    age:18,
    gender:'男'
    [Symbol()]:"测试的属性"//无法枚举，此项为使用符号添加的属性，外界无法访问
}
for(let propName in obj){
    console.log(propName,obj[propName])//(属性名，属性值)
}
```

```javascript
/*
-原始值都属于不可变量，一旦创建就无法修改
-在内存中不会创建重复的原始值
*/
/*
-对象属于可变类型
-对象创建完成后，可以任意的添加删除修改对象中的属性
-注意：
	-当俩个对象进行相等或全等比较时，比较的是对象的内存地址
	如果两个变量同时指向一个对象，则通过一个变量修改对象时，对另一个变量也会产生影响
*/
let obj1 = Object()
let obj2 = Object()//两个对象的地址不一样，内存中的值不同
console.log(obj1 === obj2)//输出结果为false
console.log(obj1 == obj2)//输出结果为false

let obj3 = obj1//地址一样，改obj3的时候obj1也会变
console.log(obj3 === obj1)//输出结果为true
console.log(obj3 == obj1)//输出结果为true

/*
修改对象：
	-修改对象时，如果有其他变量指向该对象，则所有指向该对象的变量都会受到影响
修改变量：
	-修改变量时只会影响当前的变量
在使用变量存储对象时，很容易因为改变变量指向的对象，提高代码的复杂度，所以通常情况下，声明存储对象的变量时会使用const
注意：
	-const只是禁止变量被重新赋值，对对象的修改没有任何影响
*/
```

## 14.函数

```javascript
/*
函数（function）
	-函数也是一个对象
	-它具有其他对象所有的功能
	-函数中可以存储代码，且可以在需要时调用这些代码
语法：
	function 函数名(){
        语句
    }
调用函数：
	-调用函数就是执行函数中存储的代码
	-语法：
		函数对象()
使用typeof 检查函数对象时会返回function       
*/
//创建一个函数对象
function fn(){
    xxxxxxxxxx
}
输出：console.log(fn)

/*
函数定义方法:
1.函数声明
	function 函数名(){
        	语句
    	}
2.函数表达式
	匿名的函数
	const fn = function(){
			语句
	}
3.箭头函数
	const fn = () => {
		语句
	}
*/
```

```javascript
参数

/*
定义一个可以求任意两个数和的函数
*/
function sum(){
    console.log(1+1)
}
sum()
const sum2 = function(){
	console.log(1+1)
}
sum2()
const sum3 = () => {
    console.log(1+1)
}
sum3()
//输出全是2，但是参数已经定死，无法实现“任意两个数求和的功能”
/*
在函数中不能把参数定死，而是应该在调用函数的时候再定参数
*/

/*
参数
	-在定义函数的时候，可以在函数中指定数量不变的参数
	-在函数中定义形参，就相当于在函数内部声明了对应的变量，但是没有赋值

实际参数
	-在调用函数时，可以在函数的（）中传递数量不等的实参
	-实参会赋值给其对应的形参
	-参数：
		1.如果形参和实参数量相同，则对应的实参赋值给对应的形参
		2.如果实参多于形参，则多余的实参不会使用
		3.如果形参多于实参，则多余的形参为undefined
	-参数类型
		- JS中不会检查参数的类型，可以传递任何类型的值作为参数

1.函数声明
	function 函数名([参数]){
        	语句
    	}
2.函数表达式
	匿名的函数
	const fn = function([参数]){
			语句
	}
3.箭头函数
	const fn = ([参数]) => {
		语句
	}
*/
function fn(a,b,c){
    xxxxxx
}
fn(1,2,3)//a=1,b=2,c=3,任何类型都可以作为参数
```

```javascript
this

/*
this
	- 函数在执行时，JS解释器每次都会传递进一个隐含的参数
	- 这个参数就叫做this
	- this会指向一个对象
		this所指向的对象会根据函数调用方式的不同而不同
			1.以函数的形式调用时，this指向的是window
			2.以方法的形式调用时，this指向的是调用方法的对象
			。。。。。。。
	- 通过this可以在方法中来引用调用方法的对象
*/
function fn(){
    console.log(this)//输出：window。。。。。。。
}
const obj1 = {name:"任超毅", test:fn}
obj1.test()//输出：{name:"任超毅", test:fn}
const obj2 = {name:"钟离", test：fn}
obj2.test()//输出：{name:"钟离", test:fn}

箭头函数中的this
/*
箭头函数：
	([参数]) => 返回值
例子：
	无参箭头函数：() => 返回值
	一个参数：a => 返回值
	多个参数：(a, b) => 返回值
	
	只有一个语句的函数：() => 返回值
	只返回一个对象的函数：() => ({...})
	有多行语句的函数：() => {
		........
		return 返回值
	}
	- 箭头函数没有自己this，它的this有外层作用域决定
	- 箭头函数的this和它的调用方式无关
*/
function fn1(){
    console.log("fn1--->", this)
}
const fn1(){
    console.log("fn2--->", this)
}
const obj = {
    name:"任超毅",
    fn1,//属性名和属性值相同时可以省略属性值，输出为任超毅
    fn2//输出为window
}
```

```javascript
封装一个函数可用于不同的变量，数组
创建函数：function sort（）{
	//sort后面的括号里需要传参
}

要注意的问题：
一个函数的参数也可以是函数，如果将函数作为参数传递，那么我们就称这个函数为回调函数（callback），（OCP原则，用于修改函数的时候）

高阶函数：
将函数作为参数，意味着可以对另一个函数动态的传递代码
直接在result中修改
result = sort（arr, a => age >=18）

例1：在不修改原函数的基础上为其增加记录日志的功能
function someFn(){
    return "hello"
}
/*function outer(cb){
    return () => {
        console.log("记录日志")
        const result = cb()
        return result
    }
}*/
let result = outer(someFn)
console.log(result)

例2：创建一个函数，第一次调用打印1，第二次调用打印2
方法一：
function fn(){
    let num = 0//应该定义到函数外面，就不会每次都返回num = 0
    num++
    console.log(num)
}
方法二：(闭包)
1.闭包就是能访问到外部函数作用域中变量的函数，
2.当我们需要隐藏一些不想被访问的内容时，会用到闭包，也就是说会占用一定的内存空间（相较于类，闭包较为浪费。执行次数少用类，多用闭包）
3.内部函数作为返回值返回
function outer(){
    let num = 0
    return () => {
        num++
        console.log(num)
    }
}
const fn = outer()
闭包注意事项：
function outer(){
    let x = "x"
    return function(){
        console.log(x)
    }
}
const fn = outer()
闭包的生命周期：
	1.闭包在外部函数调用时产生，外部函数每次调用都会产生一个全新的闭包
    2.在内部函数丢失时销毁（内部函数被垃圾回收了，闭包才会消失）
```

## 15.面向对象

```javascript
/*
 面向对象编程（OOP）
 	1.程序是对现实世界的抽象，不能体现出现实世界的具体信息
 	2.对象是干嘛的：
    	- 一个事物抽象到对象后就变成了对象
 		- 在程序中，一切都是对象
 	3.面向对象的编程：
 		- 面向对象的编程是指，程序中的所有操作都是通过对象来完成的
 		- 做任何事情前都需要找到它的对象，然后通过对象来完成各种操作
*/

/*
	-一个对象由两部分组成：属性和方向
	-一个事物通常有两部分组成：数据和功能
	-事物的数据到了对象中，体现为属性
	-事物的功能到了对象中，体现为方法
*/
const fn ={
    //添加属性
    name:"任超毅",
    age:18,
    
    //添加方法
    sleep(){
        console.log(this.name + "睡觉")
	}
    eat(){
        console.log(this.name + "吃饭")
	}
}
```

```javascript
类
/*
使用object创建对象的问题
	1.无法区分出不同类型的对象
	2.不方便批量创建对象

在JS中可以通过类（class）来解决这个问题
	1.类是对象模板，可以将对象的属性和方法直接定义在类中
		- 定义后，就可以直接通过类来创建对象
	2.通过同一个类创建的对象，我们称之为同类对象
		- 可以使用instanceof来检查一个对象是否是由某个类创造
		console.log(fn instanceof Person)
		- 如果某个对象是由某个类创建，则我们称该对象是这个类的实例
语法：
	class 类名 {}（类名要使用大驼峰命名法）
	const 类名 = class {}

通过类创建对象
	new 类() //调用构造函数创建对象
*/

//Person类用来创建人的对象
class Person{
    
}

//Dog类专门用来创建狗的对象
class Dog{
    
}

const p1 = new Person() //调用构造函数创建对象，p1就是Person类的实例
const p2 = new Person()

const d1 = new Dog()
const d2 = new Dog()

console.log(p1 instanceof Person)//返回true
console.log(d1 instanceof Person)//返回false
```

## 16.正则表达式

```javascript
/*
正则表达式
	- 正则表达式是用来定义表达式的规则
	- 通过这个规则计算机可以检查一个字符串是否符合规则
		或者将字符串中符合规则的内容提取出来
	- 正则表达式也是一个对象
		所以使用的时候，需要先创建正则表达式的对象
		
*/

//new RegExp()可以接收两个参数（都是字符串）1.模式字符串（正则表达式）2.标识（匹配模式）
let reg = new RegExp("a", "i") // 通过构造函数来创建一个正则表达式的对象

//使用字面量来创建正则表达式：/正则/匹配模式
reg = /a/i

//注意：
reg = /\w/ <===> reg = new RegExp("\\w")

//通过正则表达式检查一个字符串是否符合规则
reg = new RegExp("a")//检查一个字符串中是否有a
let str = "abc"
let result = reg.test(str)
console.log(result)//返回true
```

```javascript
正则表达式的语法
/*
语法
	1.在正则表达式中大部分字符都可以直接写
	2.| 在正则表达式中表示“或”
	3.[]表示或（字符集）
	4.[^]表示除了
	5..表示除了换行符的任意字符
	6.在正则表达式中使用\作为转移字符
	7.其它字符集
		\w 任意的单词字符，可以理解为[A-za-z0-9_]
		\W 除了单词字符，可以理解为[^A-za-z0-9_]
		\d 任意数字[0-9]
		\D 除了数字[^0-9]
		\s 空格
		\S 除了空格
		\b 单词边界
		\B 除了单词边界
	8.开头和结尾
		^表示字符串开头
		$表示字符串结尾
*/
re = /a|b|c|d/<===>re = /[abcd]/
re = /[a-z]/ //任意小写字母
re = /[A-Z]/ //任意大写字母
re = /[a-zA-Z]/ //任意字母
re = /[a-z]/i // 匹配模式i表示忽略大小写
re = /[0-9]/ //任意数字

re = /[^a-z]/ //匹配包含小写字母以外内容的字符串

re = /./ //匹配除了换行符以外的任意字符

re = /\./ //只匹配.

re = /^a/ //匹配开头位置的a
re = /a$/ //匹配结尾位置的a
re = /^a$/ //只匹配字母a，完全匹配，要求字符串必须和正则完全一致
```

```javascript
量词
/*
量词
	- {m}正好m个
	- {m,}只少m个
	- {m,n}m-n个
	- + 1个以上，相当于{1,}
	- * 任意数量的字符
	- ? 0-1次，相当于{0,1}
*/
re = /^a{20}$/ //正好20个a，多了不行，少了也不行
//注意： 量词只对前一个字符起作用
re = /^ab{20}$/ //20个b，但是不管a
re = /^(ab){20}$/ //20个ab

re = /^[a-z]{3}$/ //匹配3个随机小写字母，等同于re = /^[a-z][a-z][a-z]$/

re = /^[a-z]{1,}$/ //匹配1个及以上的小写字母
re = /^[a-z]{1,3}$/ //匹配1个及以上3个以内的小写字母
```

```javascript
re.exec()
/*
re.exec()
	- 获取字符串中符合正则表达式的内容
*/
let str = "abcaecafc"
// 提取出str中符合axc格式的内容

// g表示全局匹配
let re = /a[a-z]c/ig
let result = re.exec(str)
console.log(result)
```

